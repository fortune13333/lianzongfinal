---
title: 图3-3 “保存会话并审计”操作序列图
---

[图表建议 - 类型: 生成图]
[图表标题: 图3-3 “保存会话并审计”操作序列图]
[图表描述: 绘制一张UML序列图。参与者（Lifeline）包括：用户（Actor）、前端UI、后端API、网络设备、Gemini AI、数据库。序列从用户点击“保存并审计”按钮开始，详细展示消息的传递顺序：1. 前端向后端发送API请求。2. 后端连接网络设备获取配置。3. 后端向数据库查询历史数据和策略。4. 后端调用Gemini AI进行审计。5. 后端构建新区块并写入数据库。6. 后端向前端返回成功响应。7. 前端刷新UI。]

#### **生成代码 (Mermaid)**

```mermaid
%%{init: {'theme': 'neutral', 'fontFamily': 'sans-serif', 'fontSize': '25px', 'sequence': {'actorMargin': 25}, 'themeCSS': 'rect.actor, rect.participant, line.actor-line, path, rect.actor-activation, rect.activation, .arrowheadPath, .arrowhead, .messageLine0, .messageLine1, .alt-box, .alt-box div, .alt-box rect { stroke-width: 5px; font-weight: bold; } text, .label text, .actor text, .note text, .messageText { font-weight: bold; }'}}%%

sequenceDiagram
    participant U as 用户
    participant FE as 前端 UI (React)
    participant BE as 后端服务 (FastAPI)
    participant D as 网络设备
    participant DB as 数据库 (SQLite)
    participant AI as AI模型 (Gemini)

    U->>FE: 1. 点击“保存会话并审计”
    activate FE
    note right of FE: (前端弹出分步进度提示)
    FE->>BE: 2. POST /api/sessions/save
    activate BE
    
    BE->>D: 3. (SSH) 获取<br> running-config
    activate D
    D-->>BE: 4. 返回最新配置
    deactivate D
    
    BE->>DB: 5. 查询上一区块 & 合规策略
    activate DB
    DB-->>BE: 6. 返回数据
    deactivate DB
    
    alt 审计通过
        BE->>AI: 7. 执行合规性审计 (通过)
        activate AI
        AI-->>BE: 8. 返回报告
        deactivate AI
        
        BE->>AI: 9. 执行智能分析
        activate AI
        AI-->>BE: 10. 返回结果
        deactivate AI
        
        BE->>BE: 11. 构建新区块, 计算哈希
        
        BE->>DB: 12. 写入新区块
        activate DB
        DB-->>BE: 13. 写入成功
        deactivate DB
        
        BE->>DB: 14. 记录审计日志
        activate DB
        DB-->>BE: 15. 记录成功
        deactivate DB
        
        BE-->>FE: 16. 返回成功响应 (200 OK)
        FE-->>U: 17. 提示“保存成功”
        
    else 审计失败
        BE->>AI: 7a. 执行合规性审计 (失败)
        activate AI
        AI-->>BE: 8a. 返回报告
        deactivate AI
        
        rect rgb(255, 228, 225)
            BE-->>FE: 9a. 返回失败响应 (400)
            FE-->>U: 10a. 提示“操作失败”
        end
    end
    
    deactivate BE
    deactivate FE
```